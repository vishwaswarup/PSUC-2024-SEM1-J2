#include <stdio.h>
// ASCII Value of Character: Print the ASCII value of a character. 
int main(){
    char ch;
    printf("enter character : ");
    scanf("%c",&ch);
    printf("the ASCII character for %c is %d\n", ch, ch);
    return 0;
}
